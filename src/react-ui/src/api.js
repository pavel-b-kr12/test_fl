//FAS for default colors timer

// const fasUrl = process.env.REACT_APP_FAS_URL;

// export const setTimer = () => fetch(`${fasUrl}/reset`, {
  // headers: { 'Content-Type': 'application/json' }
// });
